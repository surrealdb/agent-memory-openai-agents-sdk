[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-memory-openai-agents-sdk"
version = "0.2.0"
description = "Agent Memory for the OpenAI Agents SDK"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "SurrealDB", email = "hello@surrealdb.com" }]
keywords = [
    "surrealdb",
    "agent-memory",
    "openai",
    "agents",
    "memory",
    "llm",
    "ai",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "openai-agents>=0.0.1",
    "surrealdb[memory]>=3.0.0b8",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[project.urls]
Homepage = "https://surrealdb.com/agent-memory"
Documentation = "https://surrealdb.com/docs/agent-memory"
Repository = "https://github.com/surrealdb/agent-memory-openai-agents-sdk"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_memory_openai_agents_sdk"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
